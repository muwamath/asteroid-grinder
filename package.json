{
  "name": "asteroid-grinder",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "An idle physics sandbox: pinatas fall, weapons chop, upgrades compound. Phaser 3 port of Pinata Grinder.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
